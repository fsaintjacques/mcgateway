image:
  repository: localhost/mcgateway
  tag: dev
  pullPolicy: Never

gateway:
  liveReload: true

backends:
  enabled: true
  image:
    repository: memcached
    tag: "1.6.32-alpine"
    pullPolicy: IfNotPresent
